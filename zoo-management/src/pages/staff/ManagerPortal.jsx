// ManagerPortal removed on 2025-11-07 per user request.
// Kept as a harmless stub so accidental imports won't break the dev server while
// the repository stabilizes. If you prefer the file deleted entirely, I can
// remove it once the working copy is committed and no external process restores it.

export default function ManagerPortalRemoved() {
  return null;
}
